# Pins EXACTS (==) issus de l'environnement testé (.venv) — graphe déterministe,
# zéro backtracking. Fix du CI `resolution-too-deep`.
pytest==8.4.2
pytest-asyncio==0.26.0
respx==0.23.1

# --- Sous-dépendances critiques (pins exacts → résolution rapide & déterministe) ---
pluggy==1.6.0
iniconfig==2.3.0
packaging==26.3
exceptiongroup==1.3.1

# freezegun
# pytest-cov
