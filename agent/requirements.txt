# Pins EXACTS (==) issus de l'environnement testé (.venv) — graphe déterministe,
# zéro backtracking. Fix du CI `resolution-too-deep` : les plages ouvertes
# (ex. uvicorn>=0.34,<1.0) faisaient backtracker pip sur 20+ versions.
fastapi==0.141.1
uvicorn[standard]==0.52.4
docker==7.2.0
pyyaml==6.0.3  # pin: wheel cp312 précompilé (évite build sdist Cython)
httpx==0.28.1
websockets==17.1

# --- Sous-dépendances critiques (pins exacts → résolution rapide & déterministe) ---
starlette==1.6.0
pydantic==2.13.4
pydantic_core==2.46.4
typing_extensions==4.16.0
anyio==4.14.2
annotated-types==0.8.0
h11==0.16.0
httpcore==1.0.9
certifi==2026.7.22
idna==3.19
click==8.4.2
uvloop==0.22.1
httptools==0.8.0
watchfiles==1.2.0
urllib3==2.7.0
requests==2.34.2
